name: Java CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v3
      with:
        fetch-depth: 0

    - name: Set up JDK 11
      uses: actions/setup-java@v3
      with:
        java-version: '11'
        distribution: 'temurin'

    - name: Build with Maven
      run: mvn -B package --file pom.xml

    - name: Run tests
      run: mvn test

    - name: Move JAR to Root
      run: mv target/*.jar ./

    - name: Commit and Push JAR to main
      run: |
        git config --global user.name "github-actions"
        git config --global user.email "github-actions@github.com"
        git add *.jar
        git commit -m "Add built JAR file"
        git push origin main
      env:
        GITHUB_TOKEN:  ${{ secrets.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_TOKEN }}  # 使用你刚刚添加的 PAT
